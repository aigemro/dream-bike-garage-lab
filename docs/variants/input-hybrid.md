# 탭·드래그 하이브리드 입력

## 구현 방식
짧은 포인터 입력은 탭으로, 이동 임계값을 넘으면 드래그로 판정합니다. 두 방식은 동일한 이동·머지 명령을 호출합니다.

- 이동 임계값: 12px
- 임계값 미만은 탭 선택·목적지 지정
- 임계값 이상은 직접 드래그 앤 드롭
- 두 제스처 모두 동일한 이동·머지·오류 처리 함수 사용

## 확인할 점
탭과 드래그의 오인식, 사용자의 선택권과 학습 부담, 모바일·데스크톱 일관성과 테스트 복잡도를 확인합니다.
